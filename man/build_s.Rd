% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_s.R
\name{build_s}
\alias{build_s}
\title{Rebuild shape from amplitude and phase values}
\usage{
build_s(x, sing.vals = F, fou.pars = F, npts)
}
\arguments{
\item{x}{Either a data frame or vector containing amplitude and phase values.
If \code{sing.vals = TRUE}, \code{x} is a data frame with rows equal to the number of harmonics and 4 columns:
1: Ax (amplitude x), 2: Ay (amplitude y), 3: Phix (phase x), 4: Phiy (phase y).
If \code{sing.vals = FALSE}, \code{x} is a vector of values in the order: Ax1, Ay1, Ax2, Ay2, ..., Axn, Ayn, Phix1, Phiy1, Phix2, Phiy2, ..., Phixn, Phiyn.}

\item{sing.vals}{Logical. Indicates if \code{x} contains singular values as a data frame (\code{TRUE}) or a combined vector (\code{FALSE}). Default is \code{FALSE}.}

\item{fou.pars}{Logical. If \code{TRUE}, the function returns a list containing the Fourier parameters and the reconstructed shape coordinates. Default is \code{FALSE}.}

\item{npts}{Integer. Number of points to use in the reconstructed shape.}
}
\value{
If \code{fou.pars = FALSE}, returns a matrix of shape coordinates (x,y).
If \code{fou.pars = TRUE}, returns a list with two elements:
\enumerate{
\item \code{Fourier_parameters}: data frame of Fourier coefficients used, with rows for harmonics and columns \code{an}, \code{bn}, \code{cn}, and \code{dn}.
\item \code{Shape}: matrix of shape coordinates (x,y).
}
}
\description{
Reconstructs a shape from given amplitude and phase values of its harmonics.
Optionally returns the Fourier parameters used for reconstruction.
}
