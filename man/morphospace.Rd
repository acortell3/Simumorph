% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morphospace.R
\name{morphospace}
\alias{morphospace}
\title{Create morphospace from shape outlines}
\usage{
morphospace(
  x,
  expand = F,
  mode = "mean",
  cov.mat = T,
  n.harm,
  type.names = NULL
)
}
\arguments{
\item{x}{A \code{coo} object (from \pkg{Momocs}) containing the shapes.}

\item{expand}{Logical. Whether to impute new shapes between existing ones. Default is \code{FALSE}.}

\item{mode}{Character. Only used if \code{expand = TRUE}. Currently supports \code{"mean"} which imputes mean shapes between pairs of shapes. More modes may be added in the future.}

\item{cov.mat}{Logical. Whether to compute and return the covariance matrix. Default is \code{TRUE}.}

\item{n.harm}{Integer. Number of harmonics to use.}

\item{type.names}{Optional character vector with type names for each shape (length must match \code{length(x)}). If provided, these names will label the rows.}
}
\value{
If \code{cov.mat = FALSE}, returns a list with:
\item{Fourier_parameters}{Fourier parameters of \code{x}.}
\item{amp_phase}{Data frame with amplitudes and phases for each shape.}

If \code{cov.mat = TRUE}, returns a list with the above plus:
\item{covariance_matrix}{Covariance matrix of the amplitude and phase values.}
}
\description{
Creates a morphospace from a set of shapes by extracting their amplitude and phase values.
Optionally, it can also compute the covariance matrix of these parameters.
}
