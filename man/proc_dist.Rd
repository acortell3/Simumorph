% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proc_dist.R
\name{proc_dist}
\alias{proc_dist}
\title{Compute Procrustes distances}
\usage{
proc_dist(shape1, shape2, multi = F)
}
\arguments{
\item{shape1}{A data frame with two columns (x and y coordinates) defining one shape.}

\item{shape2}{Either a data frame (if \code{multi = FALSE}) or a list of data frames (if \code{multi = TRUE}) with x and y coordinates.}

\item{multi}{Logical. If \code{TRUE}, \code{shape2} must be a list of shapes, and distances are computed from \code{shape1} to each element. Default is \code{FALSE}.}
}
\value{
If \code{multi = FALSE}, returns a numeric value: the Procrustes distance between \code{shape1} and \code{shape2}.
If \code{multi = TRUE}, returns a numeric vector of distances between \code{shape1} and each shape in the list.
If the list is named, the result will be a named vector.
}
\description{
Computes Procrustes distances between two shapes after aligning their x and y coordinates using Generalized Procrustes Analysis (GPA).
If \code{multi = TRUE}, distances are computed between \code{shape1} and each element in a list of shapes.
}
